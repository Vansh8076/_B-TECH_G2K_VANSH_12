#include <stdio.h>
int gcd(int a , int b){
    if(b==0 ){
        return a;
    }else{
        return gcd(b,a%b);
    }
}
int main() {
    int a,b;
    printf("Enter the value of a,b\n : ");
    scanf("%d%d",&a,&b);
    int result=gcd(a,b);
    printf("The GCD is = %d", result);
    return 0;
}
